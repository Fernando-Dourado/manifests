service: fd-hello-sls-v3

frameworkVersion: '3'

provider:
  name: aws
  runtime: nodejs18.x
  region: us-east-1
  stage: production

functions:
  hello:
    handler: handler.hello
    events:
      - http:
          path: hello
          method: get

# plugins:
#   - serverless-offline
